@model IEnumerable<CaixaATM.Models.Conta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Saldo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SaldoProv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoDeConta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Saldo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SaldoProv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoDeConta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ContaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ContaID })
        </td>
    </tr>
}

</table>
